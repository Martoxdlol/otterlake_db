[package]
name = "database"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1.89"
storage = { path = "../storage" }
thiserror = "2.0.18"
tokio = { version = "1.0", features = ["full"] }
